function demo () {
  console.log('demo');
}

const suma = (a, b) => a + b;

// function multiply (a, b) {
//   return a * b;
// }

const d = (a, b) => {
  console.log('dividing');
  // console.log(a, b);
  return a / b;
};

// const obj = {
//   name: 'example',
//   age: 30,
// };

const get_name = (obj) => obj.name;